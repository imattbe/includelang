This is includelang, a compiled meme language designed to use the absolute worst parts of import heavy languages. It will intially be compiled in C++ and then using itself later.

These are some initial concepts of how includeLang will function (name in pascal case):

include as as as
include datatypes.integers.unsigned.16bit as uint16
include dataypes.integers.operators.equals as =
include dataypes.integers.operators.equivelance as ==
include datatypes.char as char
include datatype.string as string
include conditionals.if as if
include variables as var
include functions as func
include output.print.string as printS

func 🤠 uint16 num1 uint16 num 2
  if num1 == num2
    printS "They're the same"
  num1 = 4
